const initialModel = {
    modal: {
        id: 'modal',
        showCloseButton: false,
        showHeader: false,
        show: true,
    },

    iframe: {
        id: 'join'
    }
};

export default initialModel;
